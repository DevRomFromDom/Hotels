import React from "react";
import styles from "./Favorites.module.scss";

const Favorites = () => {
    return <div className={styles.favorites__wrapper}>favorites</div>;
};

export default Favorites;
