import React from "react";
import styles from "./SearchForm.module.scss";

const SearchForm = () => {
    return <div className={styles.searchform__wrapper}>SearchForm</div>;
};

export default SearchForm;
